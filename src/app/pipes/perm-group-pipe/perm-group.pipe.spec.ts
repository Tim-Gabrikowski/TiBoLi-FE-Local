import { PermGroupPipe } from './perm-group.pipe';

describe('PermGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new PermGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
